# 스탯 보스 MVP 제작 History

---

## 제작 틀 (3단계)

지난 미니게임 때처럼 크게 3단계로 나눔. 순서는 그대로(로직 → 흐름 → 렌더링) — 판정 수치가 안 잡힌 상태에서 화면부터 만들면 나중에 다시 뜯어야 하는 부분이 많았어서.

### 1단계 — 판정 로직 · 데이터 구조

화면 없이 순수 로직/데이터만. 콘솔이나 유닛 테스트로 검증 가능한 상태를 목표로 함.

- [x] 그리드(타일) 기반으로 결정 변경
- [x] 격자 정의: 바닥을 7열×5행 타일로 분할. 캐릭터는 자유 이동, 판정 시에만 좌표→타일 변환
- [x] 판정 shape 프리미티브: `cell` 단일 칸 / `row`·`column` 한 줄 / `diagonal` 대각선 / `block` 사각 범위 / `inverse` 지정 칸 제외
- [x] 패턴 데이터 구조: 5종 패턴(①단일 저격 ②광역 확산 ③좌우 스윕 ④다중 락온 ⑤연속 콤보)을 `getSteps(ctx)` 형태로 정의
- [x] 패턴별 shape 매핑: ①`cell` / ②`inverse` / ③`column` 순차 이동 / ④`cell`×2~3 고정 / ⑤`diagonal`→`block` 2step
- [x] 스탯 공식: 데미지 = 기본값×(1+공격력×계수) / 피격 = 보스공격력−방어력(최소1) / 최대HP = 체력 스탯 기반
- [x] 회피 판정: 예고 종료 시점 위치가 위험 칸 밖이면 성공
- [x] 경직·반격 판정: 경직 시간(예: 1.2초) 내에만 반격 유효
- [x] 난이도 스케일링: 0~15초(①만,1.5초) / 15~40초(①②③,1.2초) / 40초~(전체,0.8초→하한0.5초)
- [x] 운영 총 체력 고정: 1~5인 모두 HP 1,000 (과거 인원별 배율 규칙 대체)
- [x] ③ 좌우 스윕 step별 예고 시간 — 첫 step은 기본값, 이후는 1/3(최소300ms)로 가정 (임시)
- [x] 재도전 규칙: 실패해도 스탯 비감소 — encounter.js가 외부 스탯을 직접 못 건드리는 구조로 보장. 단 시간초과 기준값은 v3에서 아직 못 찾음(임시: 무제한)

### 2단계 — 웨이브 진행 · 게임 상태 흐름

- [x] 예고→회피→경직→반격→다음 패턴 루프 (`encounter.js`)
- [x] 경과 시간별 패턴 랜덤 스케줄러
- [x] 공통 게임 상태(`READY→RUNNING↔PAUSED→RESOLVING→COMPLETED`) 재사용 (`state.js`)
- [x] pause 처리: RUNNING 아닐 때 tick 무시 → 타이머 자동 정지
- [x] Battle lifecycle 계약: `init/start/pause/resume/restart/destroy` + `onComplete`
- [x] 결과 candidate 구성: `status/score/failureReason/metrics/reward`

### 3단계 — 화면 렌더링 · 입력 연결

- [x] 격자 바닥 렌더링 + 예고/공격/경직 연출, 위험 타일 하이라이트
- [x] 보스 HP바, 플레이어 HUD, 남은 시간 표시 (단, "남은 시간"이 아니라 "경과 시간"임 — 논의 사항 ①과 연동)
- [x] 공용 캐릭터 시스템 이동·공격 API 연동
- [x] PC/모바일 입력 연결 (터치 버튼 44×44px 이상) — 조이스틱 100~124px, 공격 버튼 78~94px
- [ ] 처치 연출 → 결과 화면 전환 — 처치 연출 자체가 아직 없음(승패 갈리면 그냥 멈춤). "결과 화면"은 공용 result-overlay.js 몫
- [ ] 결과 화면 UI: 클리어 시간·피해·반격 수, 재도전/허브 버튼 — 데이터(candidate.metrics)는 준비 완료, 화면은 공용 result-overlay.js 몫(라벨 등록 필요)

---

## History Log

(최신 기록이 맨 위)

### 2026-09-16 — 팀 플레이테스트 피드백 4건 반영

**여태까지 한 일**
- (윤서) 캐릭터/발판이 커 보인다는 피드백 → 격자를 7×5 → 9×6으로 더 촘촘하게 조정 (`grid.js`, `battle.js`의 DEFAULT_ARENA 좌표계도 함께 갱신)
- (유정) "첫 패턴(단일 저격)이 한 칸만 공격해서 너무 쉽다" → 단일 저격으로 판정된 칸은 회피 성공/실패와 무관하게 계속 위험한 칸으로 누적되도록 변경(`patterns.js`의 `accumulates` 플래그 + `encounter.js`의 `accumulatedHazardCells`). 격자 전체가 막혀서 클리어 불가능해지는 걸 막기 위해 전체 칸의 60%까지만 누적되도록 상한을 둠
- (지안) "반격 어떻게 하는지 이해가 힘들었다" → 화면에 항상 보이는 조작 안내 한 줄(`stat-boss-control-hint`) 추가 + 경직 중 프롬프트 문구를 "반격!"에서 "공격 버튼으로 반격!"으로 구체화 + 경직 중엔 공격 버튼 자체가 펄스로 빛나도록 강조(`view.js`, `css/stat-boss.css`)
- (윤서) "공격 패턴이 단조롭다" → 패턴 선택 방식을 완전 독립 랜덤에서 "셔플 백" 방식으로 변경. 지금 시간 구간에서 등장 가능한 패턴을 전부 한 번씩 다 쓰기 전엔 같은 패턴이 다시 안 나오게 함(`encounter.js`의 `_pickNextPattern`/`_shuffle`). 백 경계에서 같은 패턴이 우연히 두 번 연달아 나오는 경우도 별도로 방지
- 반격 가능 위치를 보스 근처로 제한하자는 의견(윤서)은 컨트롤 보스 기획과 겹쳐서 이번엔 반영 안 함(기존 결정 유지) — 대신 조작 안내를 강화하는 쪽으로 대응
- 발판 회피 시간이 짧다는 의견(지우)은 확인 결과 의도된 동작(주변 1~2칸 이동으로 회피 가능)이라 별도 수정 없음
- self-check에 패턴 다양성 검증 테스트 추가 (40초 이후 구간에서 같은 패턴 연속 금지 + 5종 등장 횟수가 최대 1개 차이로 고르게 분배됨을 확인)

#### 실플레이 중 발견한 버그 2건 이어서 수정
- **버그 수정: 광역 확산(area-burst)의 "안전 칸"이 이미 영구 위험 칸(누적형 단일 저격이 쌓아온 칸)과 겹칠 수 있었음.** "안 닿았는데 왜 죽었지" 리포트로 확인 - 이 패턴이 안전하다고 알려준 칸이 사실 예전에 누적된 위험 칸이면, 화면엔 빗금 없이 안전해 보여도 실제 판정에선 그 칸도 위험 칸 취급이라 맞았음. `patterns.js`의 area-burst가 안전 칸 후보를 고를 때부터 `accumulatedHazardCells`를 제외하도록 수정(`encounter.js`가 ctx로 넘겨줌). 반경 1칸 안이 전부 이미 위험 칸인 극단적인 경우를 대비한 폴백도 추가.
- **버그 수정(UX): 캐릭터 액터가 격자 칸보다 위 레이어(z-index 20 vs 1)라서, 내가 지금 서 있는 칸이 위험/누적위험 칸이어도 그 표시가 내 캐릭터에 완전히 가려서 안 보였음.** "안 닿았는데 왜 죽었지" 리포트의 또 다른 원인으로 추정 - 실제로는 위험 칸 위에 서 있었는데 내 캐릭터 그림이 그 칸 색을 덮어서 못 봤을 가능성. 지금 서 있는 칸이 위험하면 캐릭터 자체에 빨간 경고 광채가 켜지도록 해서, 뭐가 가리든 항상 보이게 함(`view.js`/`css/stat-boss.css`의 `[data-on-hazard]`).
- 위 2건 회귀 방지용 테스트 추가
- **버그 수정: 연속 콤보(combo-strike) 2번째 step(사각 블록)이 1번째 step(대각선) 직후
  인지할 수 없을 만큼 짧게(400ms) 나와서 겹쳐 보인다는 리포트.** 좌우 스윕은 바로 옆
  칸으로 이어져서 예측 가능하지만, 연속 콤보는 완전히 다른/무관한 위치로 갑자기
  넘어가는 형태라 같은 최소 시간으로는 부족했음. 패턴별로 다른 최소 간격을 쓸 수 있게
  하고(`encounter.js`의 `getStepTelegraphMs`가 `pattern.minStepIntervalMs`를 우선
  사용), 연속 콤보에만 `minStepIntervalMs: 700`을 지정(`patterns.js`) - "짧은 간격"
  자체는 v3 스펙 의도(콤보 챌린지)라 유지하고 최소치만 올림. 좌우 스윕 등 다른
  패턴은 기존 400ms 그대로라 영향 없음
- 위 3건 회귀 방지용 테스트 추가
- **버그 수정(내가 만든 버그 수정에서 또 발견됨): 광역 확산(area-burst) 안전 칸
  hazard 제외 수정을 처음 넣었을 때, 반경 1칸 안이 전부 이미 위험 칸이면 곧장
  "격자 전체의 아무 non-hazard 칸"으로 폴백하고 있었음 - 그 칸이 반대편 구석처럼
  아주 멀 수 있어서 "안전 칸이 1~2개 있긴 한데 예고시간 안에 절대 못 닿는다"는
  새로운 회피 불가 상황을 만들어버렸음("가끔 도달할 시간이 안 되는 경우가 있다"는
  리포트로 발견).** 반경을 1→2→3…으로 한 칸씩만 넓혀가며 "지금 닿을 수 있는 범위
  중 가장 가까운" non-hazard 칸을 찾도록 수정 - 반경1에 후보가 있으면 기존과 동일,
  없을 때만 딱 필요한 만큼만 더 멀리 찾는다(무작정 아무 데나 뽑지 않음)
- 위 버그 회귀 방지 테스트 추가 — 총 67개 전부 통과

**앞으로 할 일**
- 실제 플레이로 누적 위험 칸 상한(60%), 셔플 백, 연속 콤보 700ms 체감 난이도 재확인
- area-burst의 REACH_RADIUS_CELLS(1)가 40초 이후 구간(예고시간 800ms) 기준으로도
  여전히 타이트한 편이라(이동속도 계산상 대각선 1칸 이동에 ~810ms 필요, 반응시간은
  아예 포함 안 함) - 반경이나 예고시간을 조금 더 여유 있게 조정할지 재검토 필요
- 조작 안내 문구와 캐릭터 위험 경고 광채가 실제 화면에서 잘 보이는지(특히 모바일) 확인

**궁금한/필요한/알아낸 것**
- (알아낸 것) 캐릭터 액터 z-index(20)가 격자(1)보다 높아서, 패턴 시스템에 새 상태(누적 위험 칸 등)를 추가할 때마다 "캐릭터에 가려서 안 보이는 건 아닌지"를 매번 같이 확인해야 함
- (알아낸 것) 다중 step 패턴의 예고시간 분배 규칙(1/2, 최소 400ms)은 원래 좌우 스윕을 기준으로 튜닝된 값이라, 새로운 다중 step 패턴을 추가할 때 그대로 재사용하면 안 맞을 수 있음(패턴 성격에 따라 `minStepIntervalMs`로 개별 조정 필요)
- (변경 사항) `patterns.js`, `encounter.js`, `view.js`, `css/stat-boss.css`, `dev/self-check.mjs`
- (논의 사항) 없음

---

### 2026-09-06 — dev-harness 정리 + 모바일 버그 2건 수정 + 체크리스트 점검

여태까지 한 일

dev-harness.html만 수정: 제목·일시정지·다시 시작 버튼 항상 노출, 안내문/상태 로그는 console.log로만, "게임 설명" 팝업(? 버튼, 화면 오른쪽 위) 추가
모바일 실기기 버그 2건 수정: ① 예고↔경직 전환 시 화면 오르내리던 버그(phase-badge 줄 고정으로 해결) ② 화면이 안 들어오던 문제(모바일 전용 미디어쿼리로 여백/비율 축소, PC는 그대로)
3단계 체크리스트 점검 (위 참고, 4개 체크 완료)

앞으로 할 일

아래 논의 사항 두 가지 팀 확인 후 반영
처치 연출 추가 여부 결정
등록 연동 시 result-overlay.js에 라벨 추가 필요

궁금한/필요한/알아낸 것

(알아낸 것) 공격 패턴 무제한 반복, timeLimitSec 미설정(무제한 시간), 카드 시간 표시는 경과 시간(카운트업)
(변경 사항) view.js, css/stat-boss.css, dev/dev-harness.html
(논의 사항) ① 제한시간을 두고 줄어들게 할지(타임어택, 시간 다 되면 실패), 아니면 지금처럼 제한 없이 클리어까지 걸린 총 시간만 기록으로 측정할지
(논의 사항) ② 지금처럼 보스와의 거리 상관없이 자기 자리에서 반격 가능하게 둘지, 아니면 보스 근처로 가야만 공격 가능하게 바꿀지 (v3 명세서상 "타이밍만 맞으면 위치 무관"으로 확인됐고, 컨트롤 보스 기획서와도 비교해봤지만 아직 최종 결론은 안 남 — 재논의 필요)

---
### 2026-09-05 — grid.js 7×5 미반영 버그 수정

**여태까지 한 일**
- 2단계 코드 추가 후 self-check 56개 중 1개 실패 → 원인 추적 결과 `grid.js`의 `GRID`가 09-03에 바꾸기로 한 7×5가 아니라 여전히 `{columns:5, rows:3}`으로 남아있었음 (그때 안내받은 수정이 실제로는 로컬에 반영 안 됨)
- `GRID`를 `{columns:7, rows:5}`로 실제 수정, `self-check.mjs`에 남아있던 옛날 5×3 테스트 블록도 최신으로 교체 → 56개 전부 통과

**앞으로 할 일**
- 3단계 착수
- 앞으로 수동 수정 안내받으면 그 자리에서 바로 self-check 돌려서 반영 확인하기

**궁금한/필요한/알아낸 것**
- (알아낸 것) 수동 수정은 반영 여부를 코드가 알려주지 않아서, 안 됐어도 한동안 티가 안 날 수 있음
- (변경 사항) `grid.js` GRID 값 실제 반영, `self-check.mjs` 옛 블록 교체
- (논의 사항) 없음

---

### 2026-09-04 — 2단계(웨이브 진행·게임 상태 흐름) 코드 작성

**여태까지 한 일**
- `state.js`: 공통 게임 상태 머신 구현, 허용 전이만 표로 관리
- `encounter.js`(`StatBossEncounter`): 예고→회피→경직→반격→다음 패턴 루프 구현. `tick(deltaMs)`로 외부에서 시간 주입(테스트용), 위치는 `setPlayerPosition()`으로 외부(3단계) 갱신
- lifecycle(`init/start/pause/resume/restart/destroy`) + `onComplete(attemptId, candidate)` 전부 구현, `getSnapshot()`/`onEvent`로 3단계 연동 준비
- self-check 31개 추가, 총 56개 통과

**앞으로 할 일**
- 3단계 착수: `getSnapshot()`/`onEvent`를 화면에 연결
- 기능명세서에서 보스 공격력/기본체력/제한시간 수치 확인해서 임시값 교체

**궁금한/필요한/알아낸 것**
- (알아낸 것) encounter.js가 실제 세이브 스탯을 직접 못 바꾸는 구조라 "실패해도 스탯 안 깎임"이 설계로 보장됨
- (변경 사항) `state.js`,`encounter.js` 신규 추가, `index.js`/`self-check.mjs` 갱신
- (논의 사항) 보스 공격력·기본체력·제한시간 값을 임시값(20 / 1000 / 무제한)으로 구현 — 팀 확인 필요. 히스토리 문서는 `docs/STAT_BOSS_HISTORY.md`로 이동 예정

---

### 2026-09-03 — 격자 크기 조정 (5×3 → 7×5) 및 화면 비율 확정

**여태까지 한 일**
- 칸이 커 보인다는 피드백 → 7×5로 변경, `grid.js`/`self-check.mjs` 갱신, 25개 전부 통과
- 화면 목업도 7×5로 재작업 → "칸이 옆으로 너무 길다" 피드백 → 정사각형(31×31) 시도 → 이번엔 "너무 작다" 피드백 → 최종 76×40px(비율 1.9:1)로 확정, 보스·조작버튼 위치 재배치

**앞으로 할 일**
- 2단계 착수
- 실제 화면비 나오면 격자 비율 재조정 가능성 있음

**궁금한/필요한/알아낸 것**
- (알아낸 것) `GRID` 상수 하나만 참조하는 구조 덕분에, 칸 수 변경 시 함수는 안 건드리고 상수+테스트값만 바꾸면 됐음
- (변경 사항) `GRID` 5×3→7×5, 목업 2종 재작업
- (논의 사항) 없음

---

### 2026-09-02 (3) — 1단계(판정 로직·데이터 구조) 코드 작성

**여태까지 한 일**
- `grid.js`/`patterns.js`/`stats.js`/`difficulty.js`/`judge.js`/`random.js` 구현, `self-check.mjs`로 25개 전부 통과 확인
- 1단계 진행률 약 90% (스윕 예고시간 분배·재도전 스탯 비감소만 2단계로 이월)

**앞으로 할 일**
- 2단계 착수: 시간 루프에 연결
- 좌우 스윕 step별 예고시간 분배 방식 결정

**궁금한/필요한/알아낸 것**
- (해결됨) 다중 락온 1인 처리 → `minPlayers` 필터링으로 자동 해결
- (알아낸 것) 방어력 공식이 기획안(%감산)과 기능명세서(고정값감산) 문서 간 불일치 — 기능명세서 기준으로 구현
- (변경 사항) 전부 신규 파일 생성
- (논의 사항) 방어력 공식 불일치 팀 확인 필요

---

### 2026-09-02 (2) — 바닥·판정 방식을 그리드 기반으로 변경

**여태까지 한 일**
- 원형 판정의 방향 모호함 문제 확인 → 좀비고등학교 참고해서 사각 타일 격자 + 가로/세로/대각선/네모 판정으로 전환 결정
- 패턴 5종을 그리드 shape에 매핑, 화면 초안 재작업 (진행률 0%, 코드 작성 전)

**앞으로 할 일**
- 그리드 크기 확정(임시 5×3으로 1단계 코드 반영)
- 자유이동+판정시 변환 방식이 캐릭터 이동 API와 충돌 없는지 확인

**궁금한/필요한/알아낸 것**
- (궁금한 점) 좌우 스윕 동시 위험 열 개수 — 우선 1개씩 가정
- (변경 사항) 판정 방식 원형→그리드 셀 전면 변경
- (논의 사항) 판정 형태 변경은 스펙 재승인 불필요로 판단하고 진행

---

### 2026-09-02 (1) — 기획 확정 · 제작 틀 설정

**여태까지 한 일**
- 기능명세서 최종본 확인, 3단계 제작틀 확정, 화면 초안 1차 제작 (진행률 0%)

**앞으로 할 일**
- 1단계 착수: 패턴 5종 데이터 구조 설계
- 미확정 수치는 가밸런싱 값으로 우선 채우기

**궁금한/필요한/알아낸 것**
- (궁금한 점) 다중 락온 1인 예외 처리 방식 → 09-02(3)에서 해결
- (알아낸 것) 기능명세서 최종본에서 체력 스탯=최대HP 직결 방식으로 확정된 것으로 보임
- (변경 사항) 없음 (첫 기록)
- (논의 사항) 없음
