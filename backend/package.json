{
  "name": "ai-change-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:assets": "npm --prefix .. run build",
    "dev": "npm run build:assets && wrangler dev --config wrangler.toml",
    "deploy": "npm run build:assets && wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply ai-change --local --config wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply ai-change --remote --config wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^5.9.3",
    "wrangler": "4.127.0"
  }
}
