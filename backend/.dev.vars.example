# Copy this file to .dev.vars for local OAuth testing.
# Never commit real credential values.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Required because wrangler.toml defaults to the production origin.
# This must exactly match the local browser origin, including its port.
PUBLIC_ORIGIN=http://127.0.0.1:8787
